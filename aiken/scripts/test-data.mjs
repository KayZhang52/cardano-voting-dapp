import {
    resolvePaymentKeyHash,
    resolvePlutusScriptAddress,
    BlockfrostProvider,
    MeshWallet,
    Transaction,
    mTuple
  } from '@meshsdk/core';

let list = [1,2,3]
console.log(mTuple(list), "")


